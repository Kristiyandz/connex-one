import React, { FC } from "react";
import "./TimeDashboard.css"

export const TimeDashboard: FC = () => {
    return (
        <div data-testid="time-dashboard" className="time-db-container">
            Time Dashboard
        </div>
    );
};